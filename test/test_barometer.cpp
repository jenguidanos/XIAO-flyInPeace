#include <barometer/barometer.h>
#include <gmock/gmock.h>
#include <gtest/gtest.h>

TEST(comms, comms_transmit_expects_true)
{
    ASSERT_TRUE(true);
}

TEST(comms, comms_transmit_expects_false)
{
    ASSERT_TRUE(false);
}
